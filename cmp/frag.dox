<script xmlns="urn:dox:cmn">
	let {target, name, f} = props;
	target ||= context;
	name ||= "frag";
	let frag = renderer.x.dfrag();
	target[name] = frag;
	await props.component.toRenderBody(frag, f);
</script>
