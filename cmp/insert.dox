<script xmlns="urn:dox:cmn">
	try {
		let {nodes} = props;
		if (Array.isArray(nodes)) {
			renderer.x.aps(node, nodes);
		} else {
			renderer.x.ap(node, nodes);
		}
	} catch (e) {}
</script>
