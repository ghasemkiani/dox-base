<script xmlns="urn:dox:cmn">
	let {ctx, f} = props;
	if (ctx) {
		await props.component.toRenderBodyWith(node, ctx);
	} else {
		await props.component.toRenderBody(node, props.f);
	}
</script>
